# net-mirror

Personal config mirror. Files here are regenerated on a schedule from public community sources.

- `sub` — subscription endpoint (import directly by URL; no extension on purpose)
- `sub-stats.json` — last build summary: how many entries were dropped and why
- `build_sub.py` — the filter
